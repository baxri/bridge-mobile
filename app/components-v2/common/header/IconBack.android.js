import { Images } from 'app/themes'
import React from 'react'
import { Image } from 'react-native'

const IconBack = props => (
  <Image source={Images.icons.back_android} {...props} />
)

export default IconBack
